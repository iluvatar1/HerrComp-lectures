# Anaconda env file
# Use as: conda env create -f environment.yml
# Then activate as : conda activate herrcomp
name: herrcomp
channels:
  - conda-forge
dependencies:
  - python>=3.12
  - anaconda
  - pip
  - pip:
    - -r requirements.txt
